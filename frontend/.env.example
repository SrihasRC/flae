# Backend API service URL (Next.js server-side rewrites and SSR)
# For local FastAPI backend: http://localhost:8000
# For remote/cloud backend: http://<ip-or-host>:8000
BACKEND_URL=http://localhost:8000

# Optional: Direct client-side API URL (leave empty to proxy through Next.js rewrites)
NEXT_PUBLIC_API_URL=
